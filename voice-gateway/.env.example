# Voice Gateway environment variables
# Copy to .env and fill in values

# OpenRouter API key (required for fast + standard paths)
OPENROUTER_API_KEY=sk-or-...

# Server port (default: 8643)
# VOICE_GATEWAY_PORT=8643

# Hermes home directory (default: ~/.hermes)
# HERMES_HOME=~/.hermes
